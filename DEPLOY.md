# 🚀 Инструкция деплоя на Render + Netlify

## ✅ Что исправлено

1. **Удален проблемный постинстальный скрипт** ❌ `npm install -g youtube-dl-exec`
2. **Обновлена Node версия** с 18.x на 20.x (более стабильная)
3. **YouTube видео** теперь работает через embed-плеер (без зависимостей)
4. **Rutube и VK** продолжают работать как раньше
5. **Добавлены конфиги** для Render (Procfile, .nvmrc)

---

## 🔧 Шаг 1: Локальное тестирование

```bash
npm install
npm start
```

Откройте http://localhost:3000

---

## 📱 Шаг 2: Деплой Бэкенда на Render

1. Откройте [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите GitHub репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment**: Node.js

5. **Важно**: Скопируйте финальный URL бэкенда (например: `https://cinemate-sync-xxxx.onrender.com`)

---

## 🌐 Шаг 3: Обновить URL в коде (необязательно для локального использования)

Откройте **index.html** (строка ~835) и замените:

```javascript
// Для production Netlify - используем Render URL
if (window.location.hostname === 'filmkaif.netlify.app') {
    BACKEND_URL = 'https://ваш-новый-url-render.onrender.com';  // ← ИЗМЕНИТЕ НА СВОЙ
```

---

## 🚀 Шаг 4: Деплой Фронтенда на Netlify

1. Откройте [netlify.com](https://netlify.com)
2. Drag & Drop папку `filmSite` **или** подключите GitHub
3. Сайт будет развернут автоматически!

---

## 🎯 Что поддерживается

| Тип видео | Работает | Примечание |
|-----------|---------|-----------|
| **YouTube** | ✅ Да | Через embed-плеер |
| **Rutube** | ✅ Да | Работает везде |
| **VK видео** | ✅ Да | Embed iframe |
| **Прямые ссылки** | ✅ Да | .mp4, .m3u8, .webm |

---

## 🐛 Если что-то не работает

### Ошибка в консоли браузера?
- Откройте DevTools (F12)
- Console вкладка
- Проверьте, совпадает ли BACKEND_URL с URL вашего Render сервера

### Бэкенд не подключается?
```javascript
// Проверьте в index.html (строка 793)
console.log('BACKEND_URL:', BACKEND_URL);
```

### Render build падает?
- Убедитесь, что используете Node 20.x
- Проверьте логи: `npm install` должен пройти без ошибок

---

## 📝 Важные адреса

```
Локально:
  Фронт: http://localhost:3000
  Бэк: http://localhost:3000

Production:
  Фронт: https://filmkaif.netlify.app
  Бэк: https://cinemate-sync-xxxx.onrender.com (ВАШ URL)
```

---

## ✨ Готово! 

После деплоя сайт будет полностью работающим с поддержкой синхронизации просмотра видео в реальном времени! 🎬
